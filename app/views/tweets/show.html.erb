<%# 投稿の概要 %>
  <div class="tweet-show">
    <div class="tweet-box">
      <h2 class="name">
        <%= @tweets.name %>
      </h2>
      <div class='tweet-img-content'>
        <%= image_tag @tweets.image ,class:"tweet-box-img" %>
      </div>


      <%# ログインしているユーザーと投稿しているユーザーが、同一人物の場合と同一人物ではない場合で、処理を分けましょう %>
      <% if user_signed_in? %>
        <% if current_user.id == @tweets.user_id %>
          <%= link_to '投稿の編集', edit_tweet_path(@tweets.id), method: :get, class: "tweet-red-btn" %>
          <p class='or-text'>or</p>
          <%= link_to '削除', tweet_path(@tweets.id), method: :delete, class:'tweet-destroy' %>
        
        <% end %>
      <% end %>


      <%# //ログインしているユーザーと出品しているユーザーが、同一人物の場合と同一人物ではない場合で、処理を分けましょう %>

      <table class="detail-table">
        <tbody>
          <tr>
            <th class="detail-tweet">投稿者</th>
            <td class="detail-value"><%= @tweets.user.nickname %></td>
          </tr>
          <tr>
            <th class="detail-tweet">投稿内容</th>
            <td class="detail-value"><%= @tweets.text %></td>
          </tr>
          <tr>
            <th class="detail-tweet">カテゴリー</th>
            <td class="detail-value"><%= @tweets.category.name %></td>
          </tr>
          <tr>
            <th class="detail-tweet">地域名</th>
            <td class="detail-value"><%= @tweets.city.name %></td>
          </tr>
          <tr>
        </tbody>
      </table>
      <div class="option">
        <div class="favorite-btn">
          <%= image_tag "star.png" ,class:"favorite-star-icon" ,width:"20",height:"20"%>
          <span>お気に入り 0</span>
        </div>
        <div class="report-btn">
          <%= image_tag "flag.png" ,class:"report-flag-icon" ,width:"20",height:"20"%>
          <span>不適切な投稿の通報</span>
        </div>
      </div>
    </div>
    <%# /投稿の概要 %>
    <div class="comment-box">
    <form>
      <textarea class="comment-text"></textarea>
      <p class="comment-warn">
        相手のことを考え丁寧なコメントを心がけましょう。
        <br>
        不快な言葉遣いなどは利用制限や退会処分となることがあります。
      </p>
      <button type="submit" class="comment-btn">
        <%# <%= image_tag "comment.png" ,class:"comment-flag-icon" ,width:"20",height:"25"%> 
        <span>コメントする<span>
      </button>
    </form>
    </div>
    <div class="links">
      <a href="#" class="change-item-btn">
        ＜ 前の商品
      </a>
      <a href="#" class="change-item-btn">
        後ろの商品 ＞
      </a>
    </div>
    <a href="#" class='another-item'><%= @tweets.category.name %>をもっと見る</a>
  </div>
